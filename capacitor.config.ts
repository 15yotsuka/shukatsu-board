import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yuotsuka.shukatsuboard',
  appName: 'ShukatsuBoard',
  webDir: 'out'
};

export default config;
